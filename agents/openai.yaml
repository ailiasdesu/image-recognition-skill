interface:
  display_name: "Image Recognition"
  short_description: "Routes images through local Ollama Gemma 3 4B. NEVER embed base64 in context."
  brand_color: "#10B981"
  default_prompt: "SESSION RULE: Before responding, check if user mentions images/photos/screenshots/clipboard. If yes, immediately run ~/.codex/venv/Scripts/python.exe ~/.codex/skills/image-recognition/scripts/recognize_image.py with --clipboard or the file path. NEVER write your own image processing code. NEVER use System.Windows.Forms. NEVER embed base64."

policy:
  allow_implicit_invocation: true
