interface:
  display_name: "Image Recognition"
  short_description: "Recognize images with local vision model, output Markdown"
  brand_color: "#10B981"
  default_prompt: "Use $image-recognition to analyze this image and extract all information as Markdown."

policy:
  allow_implicit_invocation: true
